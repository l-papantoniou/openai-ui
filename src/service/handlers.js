export const handleIslandChange = (value, setState) => {
    setState(value);
};

export const handleTransportationChange = (value, setState) => {
    setState(value);
};

export const handleSeasonChange = (value, setState) => {
    setState(value);
};

export const handleBudgetOption = (value, setState) => {
    setState(value);
};

export const handleTravelerOptions = (value, setState) => {
    setState(value);
};

export const handleInputChange = (event, value, setState) => {
    setState(event.target.value);
};

export const handleLanguageOptions = (value, setState) => {
    setState(value);
};

